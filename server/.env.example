# MongoDB connection string, including the database name
MONGODB_URI=

# Must match the client's origin exactly, with no trailing slash
CLIENT_URL=http://localhost:5173

# Test-mode key only: the server refuses to start unless it starts with sk_test_
STRIPE_SECRET_KEY=
